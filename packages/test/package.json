{
  "name": "@pioneer32/smocks-test",
  "version": "0.0.1",
  "license": "MIT",
  "author": "Vlad Churakov",
  "private": true,
  "scripts": {
    "test": "FORCE_COLOR=1 npm exec -- jest --runInBand",
    "test:watch": "npm exec -- jest --clearCache && FORCE_COLOR=1 npm exec -- jest --watch",
    "typecheck": "npm exec -- tsc -noEmit"
  },
  "type": "module",
  "main": "",
  "dependencies": {
    "@pioneer32/smocks": "../smocks",
    "node-fetch": "<3",
    "office-addin-dev-certs": "^1.11.4"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.4",
    "mockdate": "^3.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
